<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <link rel="stylesheet" href="/stylesheet/dashboard.css">
    <style>
body {
    background-color: var(--dark-bg, #121212);
    color: var(--text-color, #EAEAEA);
    font-family: var(--body-font, 'Poppins', sans-serif);
}
.dashboard-container {
    max-width: 900px;
    margin: 120px auto 40px;
    padding: 2rem;
}
.dashboard-container h1 {
    font-family: var(--heading-font, 'Playfair Display', serif);
    font-size: 2.5rem;
    margin-bottom: 2rem;
    color: var(--primary-color, #D4AF37);
    text-align: center;
}
.order-table {
    width: 100%;
    border-collapse: collapse;
}
.order-table thead {
    background-color: var(--card-bg, #1E1E1E);
}
.order-table th, .order-table td {
    padding: 1rem;
    text-align: left;
    border-bottom: 1px solid #333;
}
.btn-product {
    background: transparent;
    color: var(--primary-color, #D4AF37);
    border: 1px solid var(--primary-color, #D4AF37);
    padding: 0.5rem 1rem;
    border-radius: 50px;
    cursor: pointer;
    font-weight: 500;
    transition: 0.3s ease;
}
.btn-product:hover {
    background: var(--primary-color, #D4AF37);
    color: var(--dark-bg, #121212);
}
@media (max-width: 768px) {
    .order-table thead {
        display: none;
    }
    .order-table tr {
        display: block;
        background: var(--card-bg, #1E1E1E);
        margin-bottom: 1.5rem;
        border-radius: 10px;
        padding: 1rem;
        border: 1px solid #333;
    }
    .order-table td {
        display: block;
        text-align: right; 
        padding: 0.5rem 0;
        border-bottom: none;
        position: relative;
    }
    .order-table td::before {
        content: attr(data-label);
        position: absolute;
        left: 0;
        font-weight: bold;
        color: var(--secondary-text-color, #aaa);
    }
    .order-table td:last-child {
        text-align: center;
        padding-top: 1rem;
    }
}
    </style>
</head>
<body>
    <div class="dashboard-container">
        <h1>My Order History</h1>   
        <% if(orders.length > 0) { %>
            <table class="order-table">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Product</th>
                        <th>Price</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach(order => { %>
                        <tr>
                            <td data-label="Date"><%= order.orderDate.toLocaleDateString() %></td>
                            <td data-label="Product"><%= order.productName %></td>
                            <td data-label="Price">$<%= order.price.toFixed(2) %></td>
                            <td data-label="Status"><%= order.status %></td>
                            <td data-label="Actions">
                                <% if (order.status === 'Pending') { %>
                                    <form action="/orders/cancel/<%= order._id %>" method="POST" style="margin:0;">
                                        <button type="submit" class="btn-product">Cancel Order</button>
                                    </form>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>You have not placed any orders yet.</p>
        <% } %>    
        <a href="/" class="btn" style="margin-top: 2rem; display: inline-block;">Continue Shopping</a>
    </div>
</body>
</html>
